name: Trigger auto deployment for container-app-zryfwnkf5rodw

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-zryfwnkf5rodw-AutoDeployTrigger-60fb7d30-b710-4f33-97fb-91e4cf1dcc62.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPZRYFWNKF5RODW_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPZRYFWNKF5RODW_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPZRYFWNKF5RODW_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistryzryfwnkf5rodw.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPZRYFWNKF5RODW_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPZRYFWNKF5RODW_REGISTRY_PASSWORD }}
          containerAppName: container-app-zryfwnkf5rodw
          resourceGroup: expresshello8dcabc4f
          imageToBuild: containerregistryzryfwnkf5rodw.azurecr.io/container-app-zryfwnkf5rodw:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


